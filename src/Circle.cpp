//#include "Circle.h"
//#include <SDL/SDL.h>
//
//const float PI = 3.14;
//
//void drawCircle(const Circle& circle)
//{
//	int res = 100;
//
//	for (int i = 0; i < res; i++)
//	{
//		float angle = (PI * 2 / res) * i;
//		float x = cos(angle);
//		float y = sin(angle);
//
//		float angle2 = (PI * 2 / res) * (i + 1);
//		float x2 = cos(angle2);
//		float y2 = sin(angle2);
//
//		//SDL_RenderDrawLine(render, x * circle.r + circle.x, y * circle.r + circle.y, x2 * circle.r + circle.x, y2 * circle.r + circle.y);
//	}
//}